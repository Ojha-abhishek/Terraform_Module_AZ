import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff8c00', // Change this to your primary color
    },
    secondary: {
      main: '#ff8c00', // Change this to your secondary color
    },
  },
});

export default theme;
